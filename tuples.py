# Tuple : ordered, immutable, allows duplicate elements
# main difference from list is that it cannot be changed after creation

mytuple = ("Max", 28, "Boston")
mytuple = "Max", 28, "Boston"  # parenthisis are optional
print(mytuple)